#include <bits/stdc++.h> 
using namespace std; 

int main() {
	string name;
	getline(cin,name);
    cout <<(name[0]); 

    for (int i = 1; i < name.length() - 1; i++) 
        if (name[i] == ' ') 
            cout << " " << (name[i + 1]); 
} 

 
